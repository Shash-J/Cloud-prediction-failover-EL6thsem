{"code": "// i have deleted the comments and added this new line. now it should be saved and should persist.<div>// i have added one more line to show the proof of concept</div><div>// lets go with another line\n// now i will restart the local_router and then again open it to see if the work is saved or not.\n<span class=\"keyword\">class</span> <span class=\"type\">Solution</span> {\n    <span class=\"keyword\">public int</span> <span class=\"function\">minMoves</span>(<span class=\"type\">int</span>[] nums, <span class=\"type\">int</span> limit) {\n        <span class=\"type\">int</span> n = nums.length;\n        <span class=\"comment\">// i need to find the mode of the sum number pairs which both are in the range of [1, limit]\n        // then, for each pair, check if both not in range add 2 to move</span>\n        \n        <span class=\"type\">int</span> mode = -1;\n        <span class=\"type\">HashMap</span>&lt;<span class=\"type\">Integer</span>, <span class=\"type\">Integer</span>&gt; mp = <span class=\"keyword\">new</span> <span class=\"type\">HashMap</span>&lt;&gt;();\n        <span class=\"type\">int</span> right = n - 1;\n        <span class=\"keyword\">for</span>(<span class=\"type\">int</span> left = 0; left &lt; right; left++){\n            <span class=\"comment\">// implementation...</span>\n        }\n        <span class=\"keyword\">return</span> 0;\n    }\n}\n                </div>"}